# Quiet Resolve layered-performance conformance campaign

This six-second brand-manifesto campaign exercises reusable multi-parent motion derivation through the declarative public path. It resolves an Elara-proportioned cautious gait and a separately verified rightward head turn, masks the additive layer to `neck` and `head`, requires a measurable contribution, and compiles both into an exact 72-frame performance for `character.elara-vale@0.1.4`.

The derived candidate records every input asset, artifact role, live SHA-256 digest, target-character geometry digest, layer interval, playback mode, fade, mask, and contribution threshold. Publication must independently re-hash the two source artifacts and target geometry, verify continuous base coverage, exact frame sampling, finite output, metadata lineage, skeleton compatibility, and rendered forward travel before approval state can be written.
