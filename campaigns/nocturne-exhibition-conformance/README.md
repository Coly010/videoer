# Nocturne exhibition conformance

This 20-second landscape cultural-event trailer is a deliberate generalisation test, not an extension of the rainy character benchmark. It uses the generic declarative campaign builder for procedural venue geometry, five semantic exhibition shot patterns, event typography, deterministic provider-free sound design, lighting derivation, rendering, editorial assembly, and publication evidence.

Its event lockup is a bounded derivation of the verified Rise of Demons title-treatment parent. The campaign publishes `editorial.nocturne-event-lockup@1.0.0` only after deterministic rerender, line-by-line safe-area, contrast, font-digest, live-parent, and campaign-source checks pass. The separate `nocturne-programme-announcement-reuse` campaign consumes those exact pixels unchanged, proving that the treatment is a reusable event identity rather than Nocturne-shot code.

The campaign derives `lighting.nocturne-gallery@1.0.0` from the verified legacy `environment.warm-bookshop-lighting@0.1.0` rig. The derivation may relocate, uniformly scale, recolour, and rebalance declared light roles within schema bounds. It must preserve light identity/order/type/purpose and the coherent AgX exposure contract. Build-time and approval-time verification independently recompute the expected rig; rewritten hashes cannot authorize a semantically different result.

Every shot also applies `camera-path-clearance` to the complete 1,400-triangle venue at 49 samples. The persisted Blender evidence proves that its frame-baked position and animated semantic target match the shared camera sampler with zero spatial error; rendered visibility remains a separate mandatory gate.

Run:

```sh
npm run video -- cinematic-campaign build campaigns/nocturne-exhibition-conformance/cinematic-campaign.yaml
```
